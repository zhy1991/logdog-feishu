后面的对话中描述信息请使用中文和我交流

并根据报警消息体字段内容，完善其他模块代码。
1、分析项目代码逻辑，修改企业微信收到的消息体内容：
告警主题:
告警触发主机:
告警触发时间:
告警触发关键字:
告警日志文件:
告警关键字上下文:

2、根据config_demo.yaml配置文件，完善监控的日志文件路径和监控关键字内容。一个项目中可以配置多个日志文件路径和多个监控关键字内容。
多个项目配置各自配置多个文件路径和多个监控关键字内容。都要能够发送告警。

3、所有在config_demo.yaml 配置文件中监控的日志文件程序读取时要实时记录游标位置，防止程序重启后从头读取日志文件内容，导致重复告警。

4、告警消息体中 “告警日志文件” 字段内容要显示日志文件的绝对路径；“告警关键字上下文”字段内容要显示关键字所在行的上下各5行内容。

5、config.yaml 配置文件中的多个 project-000 和 project-001 项目配置要能够同时生效，互不影响。都可以发送各自的告警。
